const numeroDeLados = 6 ;
const soma = (numeroDeLados-2) * 180

console.log(`A soma dos angulos internos é ${soma}`);

const ai = soma / numeroDeLados ;
console.log(`Caso os angulos sejam iguais ,cada um deles vale ${ai}`);