const r = 3;
const comprimento = 2 * Math.PI * r;

console.log(`O comprimento do circulo é ${comprimento.toFixed(2)}`);

const area = Math.PI * r **2;

console.log(`A area do circulo é ${area.toFixed(2)}`);